import InducedStars.Structure.Supercritical.Closeness
import InducedStars.Structure.Supercritical.FarFamily
import InducedStars.Structure.Supercritical.Penalties
import InducedStars.Structure.Supercritical.CoverReindexing
import InducedStars.Structure.Supercritical.CoverDensityBounds
import InducedStars.Structure.Supercritical.Conclusion

/-!
# Supercritical structure facade

This facade exposes the deterministic close-structure infrastructure, exact
finite far/close families, the supercritical profile/counting interfaces, and
the completed medium-degree, matching-defect, and clean sparse-set penalties
routed through `Penalties`.  The same facade now exposes the joint
cross-plus-sparse absorption comparison and its signed-shift aggregate
interface for the proof of `lemma:super-Fstar`.  It also exports the completed strictly
supercritical exact cover-reindexing and compact cross-density interfaces,
exceptional-family bound, probability limits, and paper-facing almost-all
co-multipartite theorem.
-/

namespace InducedStars

end InducedStars
