import InducedStars.Structure.Supercritical.Main
import InducedStars.Structure.Critical.FineBalanceMain
import InducedStars.Structure.Critical.FineBalanceRestriction
import InducedStars.Structure.Critical.BinomialComparison
import InducedStars.Structure.Critical.DivisionCounting
import InducedStars.Structure.Critical.AlmostAll
import InducedStars.Structure.Critical.WindowBase2

/-!
# Critical structure

The paper-facing result `inducedStarCriticalWindowBase2AlmostAll` uses the exact
floor edge count and base-two logarithms for every fixed signed window
parameter.  It
proves the sharp remainder-size concentration below and at the transition
and the co-partite conclusion above it.  The exact-density foundations,
canonical fixed-sparse fine balance, and the earlier logarithmic exceptional-
set theorem remain available as supporting results.
-/

namespace InducedStars

end InducedStars
