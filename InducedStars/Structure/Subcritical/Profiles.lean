import InducedStars.Structure.Subcritical.Basic
import InducedStars.Structure.Subcritical.ProfileCovering
import InducedStars.Structure.Subcritical.ProfileAccounting
import InducedStars.Structure.Subcritical.ProfileResidual
import InducedStars.Structure.Subcritical.ActiveModelsRecovery
import InducedStars.Structure.Subcritical.FixedCountTransfer
import InducedStars.Structure.Subcritical.ActiveLevelComparison
import InducedStars.Structure.Subcritical.ProfileLeftoverBound
import InducedStars.Structure.Subcritical.SparseRowBudget
import InducedStars.Structure.Subcritical.ProfileBoundMain

/-!
# Subcritical profiles

Finite root/row/tail profiles, canonical extraction, exact realization,
profile covering, rooted/residual defect bookkeeping, root bounds, and
residual low rows, exact active random models and recovery, fixed-count
transfer, finite signed-level comparison, and fixed-remainder leftover
enumeration, together with the singleton-safe sparse retained-row
budget. Natural-unit profile exponents, coordinate-support
independence, fixed-profile probability, rooted free energy, fixed-remainder
weighted counting, and the finite master profile bound are included.
Exponential bounds use natural units and an explicit small-alpha range. Residual safety tests induced stars avoiding all profile roots.
-/

namespace InducedStars

end InducedStars
