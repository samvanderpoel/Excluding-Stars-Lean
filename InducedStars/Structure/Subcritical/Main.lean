import InducedStars.Structure.Subcritical.Comparison
import InducedStars.Structure.Subcritical.AlmostAll
import InducedStars.Structure.Subcritical.RowConstraints
import InducedStars.Structure.Subcritical.RetainedMembership
import InducedStars.Structure.Subcritical.InternalRootPenalties
import InducedStars.Structure.Subcritical.LocalRootCardinality

/-!
# Subcritical main theorem

The deterministic cut-to-division, row-constraint, sparse-side, retained
edge-counting, finite profile, active random-model, and local root
compensation layers are available
through this façade.
Retained count vectors and root/row/tail profiles are distinct objects.
Residual matching and the fixed-optimizer-ball upper bound are also
available. The retained-key comparison, omitted-mass estimate,
distinguished-optimizer concentration, constant-factor clean comparison,
and exact subcritical disjoint-union theorem are exposed here.
-/

namespace InducedStars

end InducedStars
