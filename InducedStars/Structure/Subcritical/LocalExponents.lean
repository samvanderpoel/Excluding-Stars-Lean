import InducedStars.Structure.Subcritical.Profiles
import InducedStars.Structure.Subcritical.LocalCompensationMain
import InducedStars.Structure.Subcritical.ResidualMatchingEstimate

/-!
# Subcritical local exponents

Natural-log scalar entropy, finite local tails, exact non-low budgets,
actual high-row companions, component deficits, and unified local compensation.
The residual matching exponent is proved from actual homogeneous candidate
stars, a common coordinate polarity, and the principal Janson capability.
The common scalar reserves have a proved feasible parameter hierarchy;
the candidate radius precedes the explicit optimizer representation.
Unified local compensation uses full-degree component deficits and the zero-deficit placement comparison. The profile aggregation layer combines the local and residual penalties with one shared parameter hierarchy.
-/

namespace InducedStars

end InducedStars
