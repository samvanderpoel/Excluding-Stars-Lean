import InducedStars.Structure.Subcritical.LocalExponents
import InducedStars.Structure.Subcritical.UpperBoundMain
import InducedStars.Structure.Subcritical.GroupedUpperBound
import InducedStars.Structure.Subcritical.CleanRetainedComparison
import InducedStars.Structure.Subcritical.DistinguishedConcentration

/-!
# Subcritical fixed-optimizer-ball upper comparison

The exact clean model count, uniform profile enumeration, simultaneous
parameter hierarchy, and fixed-division/cut-ball upper bounds are exposed
here, together with the additive retained-key grouping, mass-gap and
entropy/matching comparisons, constant-factor clean lower comparison,
and concentration at the distinguished optimizer. The original valid
full-division upper bounds are unchanged.
-/

namespace InducedStars

end InducedStars
