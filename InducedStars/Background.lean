import InducedStars.Basic

/-!
# Standard background assumptions

This is the only module, besides `InducedStars.PriorLiterature`, in which a
deliberate project-specific assumption about standard background mathematics
may be declared.

The bootstrap introduces no such assumptions.  Before adding one, search
Mathlib and consider proving the required result locally.  Possible future
interface decisions include standard finite concentration estimates,
fixed-count/binomial comparison bounds, and uniform forms of Stirling's
formula.  If an assumption is ultimately necessary, document the weakest
exact statement, why Mathlib is insufficient, and every local theorem that
uses it.
-/

namespace InducedStars

end InducedStars
